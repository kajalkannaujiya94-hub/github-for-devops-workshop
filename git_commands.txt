  ls
    ls -l
    rm testing.py
    ls
    git init
    ls -a
    git status
   ls
   git status
   git add testing.py
   git status
   git rm --cached testing.py
   git status
   git add .
   git status
   git commit -m "added testing files"
   git commit -m "added testing files"
   git config --global user.email "kajalkannaujiya94@gmail.com"    
   git config --global user.name "Kajal"
   git commit -m "added testing files"
   git status
   rm testing.py
   ls
   git status
   git restore testing.py
   git status

